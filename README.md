
# Parallel Dijkstra's Algorithm (OpenMP + MPI)

This project implements Dijkstra's shortest path algorithm in sequential, OpenMP-parallel, and MPI-parallel forms.  
It evaluates performance across graph sizes and densities, measuring speedup, efficiency, and correctness.
